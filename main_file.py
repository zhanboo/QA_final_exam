# This function checks if someone is old enough to drive
def can_drive(age):
    driving_age = 16  # The legal driving age is 16
    return age >= driving_age  # Returns True if age is 16 or older, otherwise False
